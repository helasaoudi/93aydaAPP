import React from "react";
import { View, Text } from "react-native";

const ShareScreen = ({  }) => {

    return (
        <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
            <Text style={{ fontSize: 20 }}>Bienvenue dans la session  !</Text>
        </View>
    );
};

export default ShareScreen;
